<h1> ALL Angular posts</h1>

<% @portfolio_items_for_angular. each do|portfolio_item| %>
    <p><%= link_to portfolio_item.title ,portfolio_show_path(portfolio_item.id)%> </p> <%# you can remove .id from (portfolio_item.id) it works same %>
    <p><%= portfolio_item.subtitle %></p>
    <p><%= portfolio_item.body %></p>
    <%#= portfolio_item.thumb_image  image cant do this way$%>
    <p><%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %></p>
    <%= link_to "edit me", edit_portfolio_path(portfolio_item.id) %>
    <a href="/portfolios/<%= portfolio_item.id %>/edit">html style edit</a>
    <%= link_to 'Destroy', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' } %>
<%end%>
<p> all database items are <%= @portfolio_items.inspect %></p>
